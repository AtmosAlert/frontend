import { StyleSheet, Text, View } from 'react-native'

type Props = {}
const index = (props: Props) => {
  
  return (
    <View style={{backgroundClip:'red'}}>
      <Text>Home</Text>
    </View>
  )
}
export default index
const styles = StyleSheet.create({})